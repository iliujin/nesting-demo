# 验证记录

日期：2026-09-10。范围：独立公开演示前端，未接入远程求解器。

## 功能检查

- `npm test`：16 项通过，覆盖面积、负坐标边界、文本数量展开、输入配额、非有限/不匹配坐标、自交、SVG 转义、示例完整性/容器内包含/不重叠、编号位置。
- `npm run build`：Vue 类型检查和 Vite 生产构建通过。
- `npm run test:e2e`：10 项通过，桌面与手机各 5 项。覆盖编号/选择/缩放、模式切换清除旧结果、多容器切换、上传文件仅在本地解析、无效文件清除旧预览、真实 SVG/JSON 下载、说明弹窗及键盘焦点恢复。
- 浏览器捕获的未处理脚本错误为 0；两种视口均无页面横向溢出。
- 上传路径的测试未观察到非 GET 网络请求。应用中不存在远程求解请求或后端凭据。
- 示例面积独立手算：前六件为 708、834.5、522、920、783、701，合计 4468.5；后六件为 810、841、870、870、827、860，全部合计 9546.5。默认 160×100 材料的利用率显示为 59.7%。

- 生产构建预览：在 `/nesting-demo/` 子路径验证页面标题、12 个多边形、资源响应和脚本错误；没有失败的资源或脚本错误。
- 独立代码快照审查：覆盖解析边界、上传状态、隐私与示例导出，未发现实质问题；审查未独立读取仓库或重跑测试，不扩展为对部署配置的审查结论。

## 视觉检查

界面参考由内置 imagegen 生成，文件名 `exec-1ef21cf2-68c2-4a25-a959-2d0e930415aa.png`，保留在本地生成图目录。它是设计参考，不作为网页图片交付，也不包含求解结果证据。

内置 Browser/IAB 调用在加载组件时失败（无法写入 kernel assets），因此使用 Playwright Chromium。`view_image` 对参考图和浏览器截图的调用受 Windows 沙盒辅助进程故障影响，无法读取；实际通过已授权的只读文件路径和内联图像输出检查了原图与最终截图，没有将失败调用描述为成功。

桌面视口 1505×1045，与生成参考的原始宽高一致；截图为完整页面，因此包含视口下方少量页脚。手机视口 390×844，检查完整纵向页面。

| 检查项 | 参考与实际对照 | 处理 |
| --- | --- | --- |
| 布局 | 白色页头、标题、信息条、左参数栏、右画布 | 保留；实际按设计文字约束采用 1280px 内容宽度 |
| 排版 | 深色标题、明确控件文字、弱化辅助说明 | 提高控件字号，手机画布标签另设字号 |
| 配色 | 蓝色主按钮、浅冷灰画布、柔和多色零件 | 保留，无额外纹理图片或渐变遮罩 |
| 容器与留白 | 简洁边框、分隔指标、点阵画布 | 修复 SVG 百分比高度导致的页面撑高 |
| 几何与编号 | 可选择多边形、零件编号、尺寸线 | 使用真实 SVG 坐标及面积重心；按上到下顺序排列示例 |
| 文案真实性 | 原概念显示 75.0% 和 60 秒 | 改为按合成坐标计算的 59.7% 和“未运行”，避免虚构求解性能 |
| 移动布局 | 工作台需保持可操作 | 上下堆叠，无页面横向滚动；编号可辨认，支持放大 |

首屏文案对照：保留标题、导航、信息条和操作名称。记录的功能性调整为只读参数、未运行说明、JSON 导出、缩放、固定容器选择和上传空态；页脚明确真实求解尚待接入。以上是为准确表达功能边界的调整，不声称与参考图逐像素相同。

最终预览：[桌面](screenshots/desktop.png)、[手机](screenshots/mobile.png)。

## 公开内容检查

只发布独立前端仓库。人工检查待提交文件清单，并扫描源码和构建目录：没有求解器代码/二进制、私有实例、内部服务器地址、宿主绝对路径或令牌。原求解器工作区保留原有修改，本次未改动它们。

## 尚未验证的范围

真实后端任务流程、线上求解、并发与鉴权均不在本次前端验证结论中。GitHub Pages 线上状态以实际工作流和访问结果为准，首次开通可能需要仓库管理员选择发布来源。
